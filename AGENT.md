# AGENT_Tauri.md

## 1. このファイルの目的
このファイルは、本プロジェクトでAIエージェントが開発作業を行う際の行動指針、技術方針、設計原則、コメント方針、テスト方針、変更報告方針、Git運用上の注意事項を定義するためのものである。

AIは、コード生成、修正、リファクタリング、UI調整、文書補助を行う際、必ず本ファイルの内容を優先して従うこと。

本プロジェクトでは、以下を重視する。

- 軽量常駐のデスクトップアプリとしての快適さ
- 社内利用を想定した安全性
- キャラクター「ゆうこ」を中心とした体験設計
- ローカル保存を基本とした低コスト運用
- 人間によるレビュー前提のAI駆動開発
- Tauri / React / TypeScript / Rust による責務分離

---

## 2. プロジェクト概要
本プロジェクトは、ニュース取得・要約・用語解説・継続利用要素を組み合わせた、マスコットキャラクター「ゆうこ」付きのデスクトップアプリである。

特徴:

- 軽量常駐型のニュースコンパニオン
- 通常時はバックグラウンド待機
- 必要時のみ、ゆうこが画面端から登場
- メイン画面は、ユーザーが明示的に開いた場合や、ゆうこからの誘導時に表示する
- ソフト内では原文大量表示を避け、再説明文・要点整理・注目ポイント中心で表示
- 用語解説と「ゆうこ辞書」による知識蓄積
- 友情ランクや報酬解放による継続利用設計
- 将来的にカスタマイズ、ガチャ、Google Driveデータ移行などを拡張候補とする

---

## 3. 技術方針

### 3.1 採用方針
本プロジェクトのTauri版における基本技術方針は以下とする。

| 領域 | 技術 |
|---|---|
| デスクトップアプリ基盤 | Tauri v2 |
| UI | React |
| UI言語 | TypeScript |
| UIビルド | Vite |
| 中核ロジック | Rust |
| パッケージ管理 | pnpm |
| ローカル保存 | Markdown / JSON |
| AI利用 | Gemini API + MockProvider |
| エディタ | VS Code |
| Git | Git for Windows |

### 3.2 C++ / Qt / QMLの扱い
本プロジェクトでは、過去検討としてC++ / Qt / QML構成が存在するが、Tauri版では以下の方針とする。

- MVP段階ではC++を使用しない
- Qt / Qt Quick / QMLを使用しない
- C++ / Qt版の資料は別案として残す
- Tauri版資料やファイルは、必要に応じて末尾に `_Tauri` を付けて区別する
- 将来的に必要性が明確になった場合のみ、C++ sidecarの追加を検討する

### 3.3 Tauri版での基本構造
```text
Tauriアプリ
├─ React / TypeScript
│  ├─ 画面表示
│  ├─ 画面遷移
│  ├─ ゆうこ表示
│  ├─ 吹き出し
│  ├─ モーダル
│  └─ ユーザー操作受付
│
└─ Rust
   ├─ ニュース取得
   ├─ HTML抽出
   ├─ AI Provider制御
   ├─ Gemini API呼び出し
   ├─ MockProvider
   ├─ Markdown / JSON保存
   ├─ ゆうこ辞書
   ├─ 友情ランク
   ├─ 報酬管理
   ├─ 通知条件判定
   └─ セキュリティ制御
```

### 3.4 技術選定上の優先事項
技術選定や実装時は、以下の優先順位を守ること。

1. 安全性
2. 軽量性
3. 常駐時負荷の低さ
4. 応答速度
5. 保守性
6. UI表現力
7. 実装の容易さ

「見た目が派手になる」ことよりも、「軽く、安定し、人間が理解しやすい」ことを優先すること。

---

## 4. アーキテクチャ原則

### 4.1 責務分離
責務は明確に分離すること。

#### React / TypeScript側に持たせるもの
- メイン画面
- ニュース閲覧画面
- 設定画面
- ゆうこ辞書画面
- ニュース履歴画面
- カスタマイズ画面
- ガチャ画面
- ゆうこの表示
- 画面端からの登場演出
- 吹き出し表示
- 軽量プレビュー表示
- ランクアップ通知モーダル
- 画面レイアウト
- テーマ反映
- ユーザー操作受付
- Tauri command呼び出し

#### Rust側に持たせるもの
- RSS取得
- 記事HTML取得
- HTML本文抽出
- Markdown生成および保存
- JSON設定保存
- ニュース状態管理
- 既読状態管理
- お気に入り管理
- 友情ランク計算
- 報酬未通知管理
- おすすめ判定ロジック
- ゆうこ辞書管理
- 設定管理
- 通知抑制条件判定
- タイマー制御
- セキュリティ制御
- ログ管理
- 外部通信制御
- Gemini API呼び出し
- MockProvider制御
- Google Driveデータ移行の将来実装

### 4.2 ReactとRustの接続原則
- Reactは表示と入力を担当し、業務ロジックを持ちすぎないこと
- Reactから複雑な判定や状態変更を直接書かず、Rust側のTauri commandを通じて操作すること
- RustからReactへ渡すデータは、UI表示に適したDTOまたは型として整形してから返すこと
- Tauri commandは必要最小限にし、危険なファイル操作や外部通信を無制限に公開しないこと
- Reactは「何を表示するか」を扱い、Rustは「何をするか」を扱うこと

### 4.3 Tauri command設計原則
Tauri commandは、ReactとRustの境界である。  
AIは安易にcommandを増やしてはならない。

良い例:

```text
get_recommended_articles
get_article_detail
generate_article_summary
explain_selected_term
save_dictionary_entry
update_article_favorite
get_user_settings
save_user_settings
get_yuuko_notification_state
confirm_rank_up_reward
```

避ける例:

```text
read_any_file
write_any_file
fetch_any_url
execute_any_command
get_env_value
```

### 4.4 禁止事項
以下は禁止とする。

- React側にRSS取得、AI API呼び出し、ファイル保存などの中核処理を書くこと
- React側にAPIキーや秘密情報を持たせること
- Rust側の責務をReact側へ逃がすこと
- `dangerouslySetInnerHTML` を安易に使うこと
- HTMLをそのまま生表示すること
- 原文の大量転載に相当するUIを作ること
- AIの都合だけで依存ライブラリを勝手に追加すること
- 要件定義にない技術へ無断で置き換えること
- 常駐アプリとして重くなる構成を無批判に採用すること
- セキュリティ制約を独断で緩めること
- コメントなしで複雑なロジックを追加すること
- Tauri commandで任意ファイル操作や任意URL取得を許可すること
- 外部由来文字列を未検証のままUIやファイル名に反映すること

---

## 5. UI / UX原則

### 5.1 基本方針
- 通常時はバックグラウンド待機を基本とする
- ユーザーが望まない限りメイン画面を常時表示しない
- 画面は必要時のみ表示する
- 通知は可愛いが邪魔すぎないこと
- アニメーションは軽量であること
- 派手さより快適さを優先すること
- ゆうこはキャラクター体験の中心だが、作業の邪魔をしないこと

### 5.2 ゆうこに関する方針
- ゆうこは本プロジェクトの中心的存在である
- 単なる装飾として扱わない
- 登場、吹き出し、再説明、報酬通知はUX上の重要要素として扱う
- 表示や挙動は「軽く・自然に・邪魔しない」を優先する
- 報酬通知や誘導演出はイベント感を大事にするが、過度に重くしてはならない
- キャラクター表示が非表示時にも高負荷で動き続ける設計を避けること
- ゆうこ登場・通知挙動の詳細設計書に反する実装をしないこと

### 5.3 ソフト内ニュース表示方針
- ソフト内では原文本文そのものの大量表示を避ける
- 表示の主軸は以下とする
  - ゆうこの再説明文
  - 要点整理
  - 注目ポイント
  - 用語補足
  - ゆうこの意見・感想
- 原文確認は出典リンクへ誘導する
- 出典リンクはユーザー操作を挟んで開くこと
- AI出力内のURLを自動でリンク化しないこと

### 5.4 React UI実装方針
- 画面単位でコンポーネントを分ける
- 共通UI部品は `components/` に切り出す
- モーダルやポップアップは `dialogs/` に切り出す
- Tauri command呼び出しは `services/` に集約する
- TypeScript型は `types/` に整理する
- 1コンポーネントに過剰な責務を持たせない
- UI都合の一時状態と、アプリ永続状態を混同しない

---

## 6. 軽量性・性能方針
AIは実装時、常に以下を意識すること。

- バックグラウンド時に不要な描画を行わない
- 不要なタイマーやループを増やさない
- 常時ポーリングを安易に入れない
- メモリ使用量が膨らみやすい構造を避ける
- 大きなデータを毎回再構築しない
- 辞書や状態は必要な粒度でキャッシュする
- UIの更新頻度を必要最小限にする
- 重いアニメーションや無限ループアニメーションを避ける
- Reactの再レンダリングが過剰にならないようにする
- Rust側で不要な全文読み込みや再解析を繰り返さない
- 「簡単だから」という理由で重い構成を選ばない

パフォーマンス劣化を引き起こしやすい変更を行う場合は、その理由と影響をコメントや説明に残すこと。

---

## 7. セキュリティ方針
AIは、以下のセキュリティ原則を破ってはならない。

- 外部通信先は許可リスト方式で扱う
- RSS取得先、HTML取得先、AI利用先は管理対象とする
- ローカルアドレス、社内アドレス、危険スキームを無条件に取得しない
- 取得HTMLはそのまま表示しない
- Markdown表示で埋め込みHTMLや危険URLを許可しない
- APIキーや認証情報をコードへ直書きしない
- APIキーをReact側へ渡さない
- AIへ送るデータは最小限に絞る
- 本文全文、選択文字列全文、秘密情報をログに残さない
- 外部由来文字列をファイル名やUIに反映する際はサニタイズを行う
- Tauri commandで任意パスや任意URLを扱う場合は、Rust側で必ず検証する
- Tauriプラグイン追加時は、権限とライセンスを確認する

---

## 8. コメント方針

### 8.1 コメントの目的
コメントは、**人間によるレビュー、保守、学習を助けるために書く**。  
AIは「コードが動くこと」だけでなく、「人間が読んで理解しやすいこと」を重視すること。

### 8.2 コメントで優先すべき内容
コメントは特に以下を説明するために使うこと。

- なぜその処理が必要か
- このファイル / 関数 / 型 / コンポーネントの責務は何か
- どの要件に対応しているか
- 状態遷移の意図
- セキュリティ上の制約
- パフォーマンス配慮の意図
- 一見分かりにくい分岐の理由
- 将来拡張のために意図的に残している構造
- Tauri commandとして公開する理由
- React側ではなくRust側で処理している理由

### 8.3 コメントを付けるべき箇所
AIは最低限、以下にはコメントを付けること。

- ファイル先頭の責務説明
- 重要なReactコンポーネントの役割
- 重要なRustモジュールの役割
- Tauri commandの役割
- 状態遷移を扱う処理
- セキュリティ境界がある処理
- HTML抽出やMarkdown変換のようなルール依存処理
- おすすめ判定、友情ランク、辞書再利用のような判断系処理
- 常駐制御や通知抑制のような環境依存処理
- 外部通信やAI送信対象を決める処理

### 8.4 コメントを避けるべき箇所
以下のようなコメントは禁止または非推奨とする。

- 名前を見れば分かる1行処理の説明
- 逐語的な行単位コメント
- 実装を読めば分かるだけの繰り返し
- 実装と乖離しやすい長文説明
- あいまいな感想や主観だけのコメント
- AIが生成したことを示すだけのコメント

### 8.5 コメントの文体
- 日本語で簡潔に書く
- 説明は短文中心
- 「何を」より「なぜ」を優先する
- レビューする人がすぐ理解できることを優先する
- 曖昧な表現を避ける

### 8.6 コメント量の目安
- 少なすぎてはならない
- ただし書きすぎてもならない
- 複雑な関数・重要処理ブロックには意図コメントを付ける
- ファイル / 重要コンポーネント / 重要関数には役割コメントを付ける
- 1行ごとの逐語解説は避ける

---

## 9. テストと確認方針

### 9.1 基本原則
AIは変更後、可能な範囲で確認観点を明示すること。  
コードを書いて終わりにせず、最低限以下を考慮すること。

- 何を変更したか
- どの機能に影響しうるか
- 何を確認すべきか
- テスト追加が必要か
- テストを追加していない場合、その理由は何か

### 9.2 優先確認対象
特に以下を変更した場合は、確認観点を省略してはならない。

- 状態遷移
- 通知制御
- RSS取得
- HTML抽出
- Markdown生成
- おすすめ判定
- 友情ランク
- ゆうこ辞書再利用
- セキュリティ境界
- Tauri command
- React画面遷移
- 軽量常駐関連の処理
- APIキーや外部通信に関わる処理

### 9.3 UI変更時
UIを変更した場合は、少なくとも以下の確認観点を添えること。

- 表示崩れがないか
- 主要画面サイズで破綻しないか
- アニメーションが重くなっていないか
- メイン画面非表示前提を壊していないか
- ゆうこの表示が邪魔になっていないか
- React側にRust側の責務が混ざっていないか

### 9.4 Rust変更時
Rust側を変更した場合は、少なくとも以下を確認すること。

- `cargo check` が通るか
- Tauri command登録が正しいか
- エラー型や戻り値がReact側と合っているか
- ファイル保存先が安全か
- ログに秘密情報が出ていないか

### 9.5 TypeScript / React変更時
React側を変更した場合は、少なくとも以下を確認すること。

- `pnpm dev` または `pnpm tauri dev` で画面確認できるか
- TypeScript型エラーがないか
- ESLint / Prettierの指摘がないか
- 画面遷移が壊れていないか
- Tauri command呼び出し名がRust側と一致しているか

---

## 10. エラー処理とフォールバック方針

### 10.1 基本原則
AIは失敗時の振る舞いを必ず考慮すること。

- 失敗時は安全側へ倒す
- 一部機能の失敗でアプリ全体を落とさない
- ユーザーに過剰な不安を与えない文言とする
- フォールバック可能な場合は、可能な限り代替動作を行う
- React側にはユーザー向け文言、Rust側ログには調査用情報を分けて扱う

### 10.2 代表的なフォールバック
- RSS取得失敗時: 既存ローカルデータを表示継続
- HTML抽出失敗時: タイトル、出典、URL、概要中心で扱う
- AI要約失敗時: 要約なし状態でも記事情報は表示可能とする
- 用語解説失敗時: 再選択を促す補助メッセージを出す
- 辞書再利用失敗時: 必要ならAI生成へフォールバック
- 保存失敗時: リトライ可能性または失敗理由をログへ残す
- APIキー未設定時: MockProviderへ切り替える
- Tauri command失敗時: React側でクラッシュさせずエラー表示する

---

## 11. AIへの作業ルール

AIは変更時、以下の順序で考えること。

1. この変更は要件定義に合っているか
2. ReactとRustの責務分離を守れているか
3. 常駐アプリとして重くならないか
4. セキュリティ制約に反していないか
5. コメントは十分か
6. 将来の保守で困らないか
7. Tauri commandを増やす必要が本当にあるか
8. 依存ライブラリの追加が本当に必要か

### 11.1 提案時の原則
- 要件定義に反する実装を勝手に提案しない
- 不明点があれば、安易に実装を決め打ちしない
- 依存追加は最小限とする
- 可能なら既存方針に沿った実装を優先する
- 「簡単だから」ではなく「本プロジェクトに合うから」を基準に選ぶ
- 便利そうという理由だけでTauriプラグインを追加しない

### 11.2 修正時の原則
- 変更範囲は必要最小限にする
- 既存責務を壊さない
- UI変更でロジックを混ぜない
- ロジック変更でUIへ不要な影響を出さない
- 修正理由を人が理解できるようにする
- ファイルを大規模に動かす場合は、理由と影響を説明する

### 11.3 変更報告ルール
AIは変更提案または実装後、可能な限り以下を簡潔に示すこと。

- 何を変更したか
- なぜ変更したか
- どの要件または方針に対応するか
- 影響範囲
- 残る懸念点
- 必要な確認観点

---

## 12. Git / バージョン管理方針
プロジェクト管理方法が未確定でも、AIはGit利用を前提に行動すること。

### 12.1 基本原則
- 不要に広い変更を1回で混ぜない
- 1変更1責務を意識する
- 関係ないファイルへ触らない
- フォーマット変更だけをロジック変更と混在させない
- レビューしやすい差分を優先する
- `node_modules/` や `src-tauri/target/` を含めない
- `.env` やAPIキーを含めない

### 12.2 コミット粒度の考え方
AIが提案する変更は、以下を意識すること。

- UI変更はUI変更でまとめる
- Rustロジック変更はロジック変更でまとめる
- Tauri command追加は関連するReact呼び出しとセットで説明する
- リファクタリングと仕様変更を混ぜない
- セキュリティ修正は独立差分にしやすい形を優先する
- コメント整備だけの変更は可能なら分離する

### 12.3 ブランチ運用前提の注意
- 現在ブランチが不明な場合、破壊的変更を前提とした提案をしない
- マージ前提の差分として読みにくい変更を避ける
- コンフリクトしやすい広範囲変更はなるべく分割する

### 12.4 AIが避けるべきこと
- コミットメッセージを推測だけで断定的に決めること
- 意味の薄い大量変更をまとめること
- 自動生成物や一時ファイルを無差別に含めること
- 履歴が読みにくくなる大規模整形だけの提案を安易に行うこと

---

## 13. 命名規則とファイル粒度

AIは命名と粒度をなるべく統一すること。

### 13.1 TypeScript / React
- Reactコンポーネント名はPascalCaseを基本とする
- ReactコンポーネントファイルはPascalCaseを基本とする
- hooksは `useXxx` のcamelCaseを基本とする
- TypeScript型やinterfaceはPascalCaseを基本とする
- 関数名・変数名はcamelCaseを基本とする
- 画面コンポーネントは `Screen` 接尾辞を基本とする
- モーダルは `Dialog` または `Modal` 接尾辞を基本とする

例:

```text
MainScreen.tsx
NewsReaderScreen.tsx
YuukoMascot.tsx
SpeechBubble.tsx
RankUpDialog.tsx
useYuukoNotification.ts
ArticleSummary
DictionaryEntry
```

### 13.2 Rust
- Rustモジュール名はsnake_caseを基本とする
- Rust関数名はsnake_caseを基本とする
- Rust構造体・enumはPascalCaseを基本とする
- Tauri command名はsnake_caseを基本とする
- 1モジュール1責務を意識する

例:

```text
article_commands.rs
dictionary_service.rs
yuuko_notification_service.rs
get_recommended_articles
save_dictionary_entry
ArticleSummary
DictionaryEntry
```

### 13.3 ファイル粒度
- 1ファイル1責務を意識する
- 役割の異なる処理を巨大ファイルへ詰め込まない
- UIコンポーネントは再利用可能性を考慮して分割する
- Rust側サービスは責務単位で分ける
- 1つのTauri commandファイルに無関係なcommandを詰め込まない

---

## 14. 望ましいディレクトリ責務の考え方
以下は初期方針であり、実際の構成は後続設計で調整可能とする。

```text
src/
  screens/
    画面単位Reactコンポーネント

  components/
    再利用UI部品

  dialogs/
    モーダル、ポップアップ

  hooks/
    React hooks

  services/
    Tauri command呼び出しラッパー

  types/
    TypeScript型定義

  styles/
    CSS、テーマ、共通スタイル

src-tauri/src/
  commands/
    Tauri command定義

  domain/
    Article、DictionaryEntry、Friendshipなどのモデル

  services/
    ニュース取得、AI連携、辞書、友情ランク、通知制御

  repositories/
    Markdown / JSON保存

  infra/
    HTTP通信、ファイルI/O、ログ、外部連携

  app/
    アプリ状態管理
```

AIは、責務が不明確なディレクトリ構成を安易に増やさないこと。

---

## 15. 依存ライブラリ追加方針

### 15.1 基本方針
依存ライブラリの追加は最小限にする。

追加する場合は、以下を確認すること。

- 本当に必要か
- 既存技術で代替できないか
- ライセンスは問題ないか
- 保守されているか
- 依存関係が重くないか
- Tauri / React / Rustと相性がよいか
- セキュリティ上の懸念がないか

### 15.2 優先ライセンス
外部ライブラリは、以下のような扱いやすいライセンスを優先する。

- MIT
- Apache-2.0
- BSD
- ISC

以下は事前確認対象とする。

- GPL
- AGPL
- LGPL
- 商用利用条件が曖昧なもの
- ライセンス表記がないもの

### 15.3 AIがしてはいけないこと
- 勝手に大量のUIライブラリを追加すること
- 一時的な実装のために重い依存を追加すること
- ライセンス不明のパッケージを提案すること
- Tauriプラグインを理由なく増やすこと

---

## 16. 将来拡張への配慮
AIは、初期版の要件を壊さない範囲で、将来拡張しやすい構造を意識すること。

想定される将来拡張:

- デコレーション追加
- テーマ追加
- ゆうこ辞書の高度検索
- 社内ニュース混在表示
- 類似一致による辞書再利用
- より詳細なおすすめ判定
- ガチャ機能
- Google Driveデータ移行
- Googleメール連携
- Live2Dなどの表示強化
- C++ sidecarによる一部処理の外部化

ただし、将来拡張のために今の構造を過剰に複雑化してはならない。

---

## 17. 資料更新時の注意

AIがMarkdown資料を作成・修正する場合は、以下を守ること。

- Tauri版とC++ / Qt版を混同しない
- Tauri版資料には必要に応じて `_Tauri` を付ける
- 技術前提を勝手に戻さない
- C++ / Qt / QML前提の記述を残さない
- React / TypeScript / Rust / Tauri command前提で整合させる
- 過去案を消すのではなく、別資料として残す方針を尊重する
- 画面仕様そのものは既存資料を活かし、技術実装部分のみ差し替える

---

## 18. 最終原則
本プロジェクトでAIが守るべき最終原則は以下である。

- 軽く作る
- 安全に作る
- 速く作る
- 分かりやすく作る
- 要件に忠実に作る
- 責務を混ぜない
- ReactとRustの境界を守る
- 人間がレビューしやすい形で残す
- ゆうこの体験価値を損なわない

本プロジェクトのTauri版は、**React / TypeScriptで顔を作り、Rustで中身を作る** 構成を基本とする。  
AIはこの原則から逸脱しないこと。
