# AGENTS.md

Claude Code / Codex 共通の開発ルール（全タスクで必ず守る最小限）。まず本ファイルを読み、領域別の詳細は必要時に `.claude/rules/` を、タスク領域の入口は `docs/00_project/ai-context/` を読む。

本プロジェクトは、ニュース取得・要約・用語解説・継続利用要素を組み合わせた、マスコットキャラクター「ゆうこ」付きの軽量常駐デスクトップアプリ（Tauri v2）。人間によるレビューを前提としたAI駆動開発を行う。

## 0. 作業開始ルール（最重要・全タスク共通）

初期探索での利用枠（トークン）浪費を避けるため、次を守る。

- 作業開始時にリポジトリ全体を読み込まない（全設計書・`components/`・`src-tauri/` などの一括読み込みをしない）。
- タスクで指定された `docs/00_project/ai-context/<領域>.md` を最初に読む。指定がなければ `docs/00_project/ai-context/README.md` を見て該当領域を選ぶ。
- 調査は `ai-context` に列挙された「最初に読むファイル」から始める。
- 領域別の詳細ルールは `.claude/rules/<領域>.md`（§13 の対応表）を、その領域を扱うときだけ読む。Claude Code では各ルールの `paths` に一致するファイルを扱うと自動で該当ルールが読み込まれる。
- `ai-context` の「対象外」に挙げた領域には、明確な必要性がない限り触れない。
- 追加のファイル探索が必要になった場合は、「何を確認するために読むか」を短く説明してから読む。
- 実装後はコミット・pushしない（指示があるときだけ）。
- 変更内容・検証結果・未実行の検証・残る懸念を報告する。
- ニュース取得の git 外設定（`app-data/` 等・`news_sources.json` / `network_allowlist.json`）には、明示的な指示なしで触れない。
- APIキー・秘密情報を画面・ログ・戻り値・コミットへ出さない。

## 1. 技術スタック

| 領域 | 技術 |
|---|---|
| アプリ基盤 | Tauri v2 |
| UI | React / TypeScript / Next.js (App Router) |
| 中核ロジック | Rust |
| パッケージ管理 | pnpm |
| ローカル保存 | Markdown / JSON |
| AI利用 | Gemini API + MockProvider |

C++ / Qt / QML は MVP段階では使用しない（過去案は別資料として残す）。

## 2. 技術選定の優先順位

安全性 ＞ 軽量性 ＞ 常駐時負荷の低さ ＞ 応答速度 ＞ 保守性 ＞ UI表現力 ＞ 実装の容易さ。「派手さ」より「軽く・安定し・人間が理解しやすい」を優先する。

## 3. アーキテクチャの基本原則

- **React / TypeScript = 顔**（画面表示・遷移・ゆうこ表示・入力受付・Tauri command 呼び出し）。
- **Rust = 中身**（取得・抽出・AI連携・保存・判定・セキュリティ制御）。
- 境界は **Tauri command**。安易に増やさず、危険操作を無制限に公開しない。React は業務ロジックを持ちすぎない。
- 詳細な責務分担は `.claude/rules/frontend.md` / `.claude/rules/rust.md`。

## 4. 全タスクで守る禁止事項

- React 側に RSS取得・AI API呼び出し・ファイル保存などの中核処理を書く。
- React 側に APIキー・秘密情報を持たせる。Rust 側の責務を React へ逃がす。
- `dangerouslySetInnerHTML` の安易な使用 / HTML の生表示 / 原文の大量転載に相当するUI。
- 任意ファイル操作・任意URL取得の Tauri command を作る。
- 外部由来文字列を未検証のまま UI・ファイル名へ反映する。
- 依存ライブラリ・Tauri プラグインを理由なく追加する。要件にない技術へ無断で置き換える。（追加が必要なときは、名称・理由・使用箇所・既存で代替できない理由・常駐負荷・セキュリティ・ライセンス〔MIT / Apache-2.0 / BSD / ISC を優先。GPL / AGPL / LGPL や表記不明は事前確認〕を明記してから提案する。）
- セキュリティ制約を独断で緩める。常駐アプリとして重い構成を無批判に採用する。
- APIキー・秘密情報・本文全文をログ・戻り値・コミットへ出す。
- ニュース取得の git 外設定（`app-data/` / `news_sources.json` / `network_allowlist.json`）に無断で触れる。

## 5. セキュリティ（要点）

- 外部通信先は許可リスト方式。ローカル/危険スキームを無条件に取得しない。
- APIキーを React へ渡さない・直書きしない。AI 送信データは最小限。取得HTMLをそのまま表示しない。外部由来文字列はサニタイズする。
- 詳細と capability / CSP / allowlist の扱いは `.claude/rules/security.md`。

## 6. コメント方針（要点）

- 日本語で簡潔に。「何を」より「なぜ」を説明する。
- ファイル先頭の責務、重要な関数・コンポーネント・Tauri command、状態遷移、セキュリティ境界、判断系（おすすめ判定・友情ランク・辞書再利用）、ルール依存処理（HTML抽出・Markdown変換）にはコメントを付ける。
- 名前で分かる1行処理の逐語コメント、AI生成を示すだけのコメントは書かない。

## 7. テスト・検証方針

- 変更後、該当する検証を実行し、未実行は理由とともに報告する。
- フロント: `pnpm build` / `pnpm lint` / `pnpm run typecheck`、UIは Playwright（詳細 `.claude/rules/frontend.md`）。
- Rust: `cargo check` / `cargo fmt --check` / `cargo test`、大きな変更で `cargo clippy`（詳細 `.claude/rules/rust.md`）。
- 状態遷移・通知制御・取得・抽出・保存・判定・セキュリティ境界・Tauri command・画面遷移を変えたら確認観点を省略しない。

## 8. エラー処理（要点）

- 失敗時は安全側へ倒す。一部機能の失敗でアプリ全体を落とさない。フォールバック可能なら代替動作。
- React はユーザー向け文言、Rust ログは調査用情報に分ける。APIキー未設定時は MockProvider へ切り替える。

## 9. Git 運用

- 1変更1責務。関係ないファイルへ触らない。フォーマット変更とロジック変更を混ぜない。レビューしやすい差分を優先する。
- `node_modules/`・`src-tauri/target/`・`.env`・APIキーを含めない。自動生成物・一時ファイルを無差別に含めない。
- コミットメッセージを推測だけで断定しない。指示がない限りコミット・pushしない。

## 10. 変更報告

- 何を / なぜ変更したか、対応する要件・方針、影響範囲、残る懸念、必要な確認観点を簡潔に示す。

## 11. コードレビュー方針（`@codex review`）

GitHub 上の Codex コードレビューでは、実害のある指摘を優先する（Codex は `.claude/rules/` を自動では読まないため、必須方針はここに置く）。

- レビュー本文・指摘・要約・修正案は、固有名詞やコードを除きすべて日本語で書く。
- P0 / P1 相当のロジックバグ、既存仕様との不整合、セキュリティ境界の崩れ、データ破損、リグレッション、失敗時整合性を優先する。あわせて責務分離の崩れ・任意ファイル操作・任意URL取得・APIキー露出・HTML生表示の混入も確認する。
- フォーマットのみ・好みの問題・既存 lint で検出できる軽微事項は、重大な影響がない限り指摘しない。
- 指摘にはファイル・該当箇所・理由・実害・最小修正案を含める。
- 問題がない場合は、その旨と残る確認観点を短く報告する。
- 比較実装（Claude Code と Codex の比較）は、同一の仕様・資料・完了条件を与え、別ブランチ（例 `compare/claude/...`）で行い、不要な大規模リファクタリングをしない。

## 12. AI 併用時の一貫性

- Claude Code / Codex いずれでも、責務分離・命名・技術構成・ディレクトリ方針を独自判断で変えない。既存と異なる実装は理由を明記する。
- 共通ルールは本ファイルを正とする。`CLAUDE.md` は `@AGENTS.md` で本ファイルを読み込み、Claude Code 固有分だけを追記する（重複再掲しない）。片方のツール向けルールを変えたら、共通部分はもう片方にも整合させる。

## 13. 領域別の詳細ルール

Claude Code は、各ルールの `paths` に一致するファイルを扱ったときに、該当 `.claude/rules/*.md` を読み込む（`paths` のないルールは起動時に常時読み込まれるため、下記ルールには必ず有効な `paths` を設定している）。Codex は `.claude/rules/` を自動では読まないので、Codex が領域別ルールを要する場合は、この表や `ai-context` の案内から必要なファイルだけを開く。

| 領域 | 対象パス（`paths`）の例 | 参照 |
|---|---|---|
| フロント | `components/**` `app/**` `hooks/**` `lib/**/*.ts` `tests/ui/**` | `.claude/rules/frontend.md` |
| Rust / Tauri | `src-tauri/**/*.rs` | `.claude/rules/rust.md` |
| 進捗管理 / Firestore docs | `task-management/**` ＋ `developタスクチェックリスト.md` など Firestore 進捗関連の個別ドキュメント | `.claude/rules/task-management.md` |
| セキュリティ | `src-tauri/capabilities/**` `src-tauri/tauri.conf.json` `src-tauri/src/infra/**` およびAPIキー・外部通信・AI送信データを扱う個別サービス | `.claude/rules/security.md` |

- タスク領域の入口: `docs/00_project/ai-context/README.md`。
- 参考資料（常駐させず必要時に参照）: 要件定義書・MVPスコープ定義書（`docs/00_project/`）、各設計書（`docs/02_design/`）。

## 14. 最終原則

軽く・安全に・速く・分かりやすく・要件に忠実に作る。責務を混ぜず、React と Rust の境界を守り、人間がレビューしやすい形で残す。**React / TypeScript で顔を、Rust で中身を作る。**
