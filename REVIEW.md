# REVIEW.md — Claude Code レビュー方針

このファイルは、`@claude` メンションで起動する Pull Request レビュー（`.github/workflows/claude-code-review.yml`）が参照するレビュー方針である。
プロジェクト全体の開発規約は `CLAUDE.md` を最優先とし、本ファイルはそれを「レビュー観点」に絞って具体化したものである。

---

## 0. 大前提

- **レビューコメントは必ず日本語**で書く。
- このアプリは **Tauri v2 + React/TypeScript（顔）+ Rust（中身）** のデスクトップ常駐アプリである。
- 最優先価値は **既存仕様を壊さないこと（リグレッション防止）** と **安全性**。
- 指摘は**実害のあるもの優先**。件数を稼ぐためのコメントはしない。
- 確信が持てない指摘は「断定」せず、確認を促す形（推測である旨を明記）で述べる。

---

## 1. 優先して見る観点（重点）

以下を重点的に確認する。

### 1.1 共通（全変更）
- **ロジックバグ**：条件分岐の取り違え、オフバイワン、None/null・空・0件・最大値などの境界値。
- **既存仕様との不整合**：`docs/`（要件定義・基本/詳細設計・画面遷移・データ設計）や既存実装と矛盾していないか。
- **セキュリティ**：後述 1.4 を厳格に確認。
- **パフォーマンス / 常駐負荷**：常駐アプリとして重くないか（不要なポーリング・全文再読込・過剰再レンダリング・無限/重アニメーション）。
- **例外処理・フォールバック**：失敗時に安全側へ倒れるか。一部失敗で全体が落ちないか。ユーザー向け文言とログの分離。
- **テスト不足**：状態遷移・判定ロジック・境界値・セキュリティ境界にテストがあるか。無い場合は理由が妥当か。
- **影響範囲（DB/API/画面項目）**：保存フォーマット（Markdown/JSON）・Tauri command の入出力型・画面表示項目への波及。
- **リグレッションリスク**：既存の呼び出し元・既存データ・既存フローへの影響。

### 1.2 React / Rust の責務分離（このリポジトリの肝）
- React/TypeScript 側に **業務ロジック（RSS取得・AI呼び出し・ファイル保存・ランク確定・抽選確定）** が漏れていないか。
- Rust 側の責務が React 側へ逃げていないか／その逆になっていないか。
- React は「何を表示するか」、Rust は「何をするか」。境界は Tauri command。

### 1.3 Tauri command 設計
- command を不必要に増やしていないか（最小限か）。
- `read_any_file` / `write_any_file` / `fetch_any_url` のような **任意ファイル操作・任意URL取得口** を作っていないか。
- command 名は `snake_case` で動詞始まりか。React 側の呼び出し名と一致しているか。
- 戻り値DTO（camelCase）と React 型が一致しているか。

### 1.4 セキュリティ境界（厳格に）
- **API キー・秘密情報**：コード直書きしていないか。React 側へ渡していないか。ログ・エラーメッセージ・URL に出していないか。
- **外部通信**：許可リスト方式を維持しているか（RSS / HTML / AI エンドポイント）。ローカル/社内アドレス・危険スキームを弾いているか。
- **取得HTML**：そのまま表示していないか。`dangerouslySetInnerHTML` や生HTML表示・危険URLの自動リンク化が無いか。
- **外部由来文字列**：ファイル名・UI へ反映する際にサニタイズしているか。
- **AI送信データ最小化**：本文全文・選択文字列全文・秘密情報を不必要に送っていないか。

### 1.5 ゆうこ体験 / UX
- 常駐の邪魔をしていないか（非表示前提を壊していないか、通知が過剰でないか）。
- ソフト内で原文の大量転載に相当する表示になっていないか（再説明・要点・注目ポイント中心か）。

---

## 2. コメントしてほしくない観点（抑制）

以下は**指摘しない**（ノイズになるため）。

- **フォーマットのみ**の指摘（インデント・空行・クォート種別・import 並び等）。
- **lint / formatter で自動検出可能**な内容（ESLint / Prettier / `cargo fmt` / `cargo clippy` が拾うもの）。
- 重大度の低い**好み（preference）**の問題（命名の些末な好み、等価な書き換え提案など）。
- 動作・安全性・仕様に影響しない**主観的なリファクタ提案**。
- 既に CI（`ci.yml` / `test-ci.yml` / `security-ci.yml` / `secret-scan.yml`）がカバーしている範囲の再指摘。

> 迷ったら「これは実害があるか？ lint で出るか？」を基準にする。実害が無く lint で出るなら書かない。

---

## 3. 既存仕様を壊さないことを重視する方針

- 変更が **既存の呼び出し元・保存データ・画面表示・command I/F** に与える影響を必ず考える。
- 後方互換（既存 JSON/Markdown の読み込み、`#[serde(default)]` 等）を壊していないか確認する。
- 「より良い設計」でも、**要件定義・既存設計と矛盾する大規模変更**は安易に推奨しない（必要なら理由を明記して提案に留める）。
- 仕様が不明な箇所は、断定せず確認を促す。

---

## 4. レビュー出力フォーマット（推奨）

各指摘は簡潔に、以下を含める。

- **重大度**：`[重大] / [中] / [軽微] / [質問]`
- **該当箇所**：ファイルパスと行（可能なら）
- **理由**：何が問題で、どんな実害があるか
- **修正案**：具体的にどう直すか（コード片があると望ましい）

最後に、重点観点（第1章）のうち**問題が無かったものは1行で「OK」**とだけ示し、冗長な総括は避ける。
重大な指摘が無い場合は「重大な問題は見当たりません」と明言してよい。
